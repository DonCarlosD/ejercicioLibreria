{% extends 'base.html.twig' %}

{#editar libro por formulario#}
{% block title %}{{ 'editar_autor'|trans }}{% endblock %}

{% block body %}
    <div class="d-flex justify-content-center align-items-center min-vh-100" >
        <div class="card p-4">
            <h1 class="card-title text-center mb-4">{{ 'editar_autor'|trans }}</h1>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
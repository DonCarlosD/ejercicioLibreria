{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit Book</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

    {% for error in errors %}
        <div class="alert alert-danger">{{ error.message }}</div>
    {% endfor %}
{% endblock %}
